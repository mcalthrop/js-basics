// declare an array of numbers for later use
var numbers = [100, 200, 300];
// declare an array of scores, initially empty
var scores = [];
// declare an object that will be bound in order to provide context
var context = {
    scores: []
};
